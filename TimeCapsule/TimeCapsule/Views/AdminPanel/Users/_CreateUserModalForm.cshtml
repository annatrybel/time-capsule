@using TimeCapsule.Models.Dto
@model UserDto
@section Styles {
    <link rel="stylesheet" href="~/css/Modal.css" asp-append-version="true" />
}

<div class="modal fade" id="addUserModal" tabindex="-1" aria-labelledby="addUserModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addUserModalLabel">Nowy użytkownik</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div id="userFormContainer">
                    <form id="addUserForm" method="post" action="/AdminPanel/Users/CreateUser">
                        <input type="hidden" name="Id" value="" />
                        <div class="card-body">
                            <div class="form-group">
                                <label>Nazwa użytkownika <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="UserName" value="" required />
                                <span class="text-danger field-validation-error" data-valmsg-for="UserName"></span>
                            </div>
                            <div class="form-group">
                                <label>Email <span class="text-danger">*</span></label>
                                <input type="email" class="form-control" name="Email" value="" required />
                                <span class="text-danger field-validation-error" data-valmsg-for="Email"></span>
                            </div>
                            <div class="form-group">
                                <label>Rola <span class="text-danger">*</span></label>
                                <select class="form-control" name="RoleId" required>
                                    @foreach (var role in Model.AvailableRoles)
                                    {
                                        <option value="@role.Id">@role.Name</option>
                                    }
                                </select>
                                <span class="text-danger field-validation-error" data-valmsg-for="RoleId"></span>
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-save" onclick="handleFormSubmit('addUserForm')">Zapisz</button>
                            <button type="button" class="btn btn-cancel" data-bs-dismiss="modal">Anuluj</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
